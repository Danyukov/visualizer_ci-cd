env: 'local'

origin: 'http://localhost:81/api'

server:
  host: 'localhost'
  port: 8889
  maxHeaderBytes: 1
  readTimeout: 10s
  writeTimeout: 10s

database:
  username: 'postgres'
  password: 'root'
  host: 'postgres_stage'# Замените localhost на имя сервиса из docker-compose.yml
  port: '5432'
  dbName: 'visualizer-db-stage'
  SSLMode: 'disable'

jwt:
  secret: 'jwt-secret'
