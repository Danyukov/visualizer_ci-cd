env: 'prod'

origin: 'http://localhost:80/api'

server:
  host: 'localhost'
  port: 8888
  maxHeaderBytes: 1
  readTimeout: 10s
  writeTimeout: 10s

database:
  username: 'postgres'
  password: 'root'
  host: 'postgres_prod'# Замените localhost на имя сервиса из docker-compose.yml
  port: '5432'
  dbName: 'visualizer-db-prod'
  SSLMode: 'disable'

jwt:
  secret: 'jwt-secret'
